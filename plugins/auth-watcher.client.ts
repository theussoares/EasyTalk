// plugins/auth-watcher.client.ts
// Alternativa mais simples que usa onMounted na página
export default defineNuxtPlugin(() => {
  // Plugin vazio - a lógica de auth será movida para as páginas que precisam
});
